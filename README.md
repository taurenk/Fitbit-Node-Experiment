# Fitbit-Node-Experiment
Goal is to make a simple Node/Express app that allows a user to OAuth into their Fitbit account and retrieve data via the Fitbit API. 


## Setup
 
    npm install
    node server.js